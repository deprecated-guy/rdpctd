import { DragDirective } from '@ui/directives';

describe('dragDirective', () => {
	it('should create an instance', () => {
		const directive = new DragDirective();
		expect(directive).toBeTruthy();
	});
});
