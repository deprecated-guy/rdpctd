import { EventOutsideDirective } from './event-outside.directive';

describe('eventOutsideDirective', () => {
	it('should create an instance', () => {
		const directive = new EventOutsideDirective();
		expect(directive).toBeTruthy();
	});
});
