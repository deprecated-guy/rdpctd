import { ProcessCardIconDirective } from './process-card-icon.directive';

describe('ProcessCardIconDirective', () => {
	it('should create an instance', () => {
		const directive = new ProcessCardIconDirective();
		expect(directive).toBeTruthy();
	});
});
